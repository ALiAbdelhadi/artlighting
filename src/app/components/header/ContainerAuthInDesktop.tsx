import React, { ReactNode } from 'react'

const ContainerAuthInDesktop = ({ children }: { children: ReactNode }) => {
    return (
        <>
        {children}
        </>
    )
}

export default ContainerAuthInDesktop